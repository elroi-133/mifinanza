<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mifinanza">
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:targetApi="s" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label= "@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity
            android:name=".PrestamosActivity"
            android:exported="false" />

        <receiver android:name=".PrestamoNotificationReceiver" />

        <activity
            android:name=".activityVerPrestamos"
            android:exported="false" />
        <activity
            android:name=".RegistrarPagoPrestamoActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrarPrestamoActivity"
            android:exported="false" />
        <activity
            android:name=".VerRegistrosActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Aquí se añade el atributo android:exported -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IngresoActivity"
            android:exported="true" /> <!-- Añadir android:exported aquí también si es necesario -->
        <activity
            android:name=".EgresoActivity"
            android:exported="true" />
        <activity
            android:name=".PartidaActivity"
            android:exported="true" />
    </application>

</manifest>
